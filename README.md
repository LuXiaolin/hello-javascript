# hello-javascript
git wiki:

in program dir: 
git init
git add ./
git commit -m 'submit';
git remote rm orgin
git remote add origin git@github.com:luxiaolin/hello.git
git push -u origin master

git pull
